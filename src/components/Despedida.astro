---
// Despedida.astro
---

<section class="relative w-full pt-10 pb-14 px-6 overflow-hidden" id="seccion-despedida">

    <!-- SEPARADOR DORADO SUPERIOR -->
    <div class="absolute top-0 left-6 right-6 h-px bg-gradient-to-r from-transparent via-[#C2A96B]/60 to-transparent"></div>
    <!-- SEPARADOR DORADO INFERIOR -->
    <div class="absolute bottom-0 left-6 right-6 h-px bg-gradient-to-r from-transparent via-[#C2A96B]/60 to-transparent"></div>

    <div class="relative z-10 flex flex-col items-center text-center max-w-xs mx-auto">

        <!-- TÍTULO "Agradecimiento" -->
        <h2
            class="font-romantic-lovely text-[#d9c07a] text-5xl sm:text-6xl leading-tight mb-4"
            id="titulo-despedida"
        >
            Agradecimiento
        </h2>

        <!-- PUNTO SEPARADOR -->
        <div class="w-2 h-2 rounded-full bg-[#d4682a] mb-6" id="punto-despedida"></div>

        <!-- MENSAJE -->
        <p
            class="font-sans text-[#f3ead9] text-base sm:text-lg leading-relaxed"
            id="texto-despedida"
        >
            A Dios por ser pieza fundamental en nuestro matrimonio, a nuestros padres y hermanos por ser nuestro apoyo y soporte, y a cada uno de ustedes por acompañarnos en este día tan especial con su presencia y cariño incondicional.
        </p>

    </div>
</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    document.addEventListener("DOMContentLoaded", () => {
        const tl = gsap.timeline({
            scrollTrigger: {
                trigger: "#seccion-despedida",
                start: "top 85%",
                toggleActions: "play none none reset",
            },
        });

        tl.fromTo(
            "#titulo-despedida",
            { opacity: 0, y: -20 },
            { opacity: 1, y: 0, duration: 1, ease: "power2.out" },
            0,
        )
        .fromTo(
            "#punto-despedida",
            { opacity: 0, scale: 0 },
            { opacity: 1, scale: 1, duration: 0.6, ease: "back.out(2)" },
            0.3,
        )
        .fromTo(
            "#texto-despedida",
            { opacity: 0, y: 20 },
            { opacity: 1, y: 0, duration: 1, ease: "sine.out" },
            0.5,
        );
    });
</script>
